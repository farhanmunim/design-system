# No environment variables are required — this is a static wireframe with no backend.
# This file exists to satisfy the project standard; add keys here (names only, no values)
# if a Worker proxy or other backend is introduced later.
